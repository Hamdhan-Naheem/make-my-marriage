# Reference only: root scripts do not load this file.
# Optional overrides belong in each application directory:
# apps/api/.env       -> PORT and DATABASE_URL (copy apps/api/.env.example)
# apps/web/.env.local -> API_ORIGIN=http://localhost:4000
# See the per-app .env.example files.
# Never prefix private settings with NEXT_PUBLIC_.
