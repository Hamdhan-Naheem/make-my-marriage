# Optional: copy to apps/web/.env.local.
# Server-only destination for the Next.js API rewrite. No browser credentials.
API_ORIGIN=http://localhost
# Production example: https://make-my-marriage-api.vercel.app
